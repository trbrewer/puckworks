# Exact versions observed in the environment that PRODUCED the committed artifacts for ALL THREE
# papers (figures, source-data CSVs, generated evidence tables). Recorded from `pip freeze` of
# that environment and used as a `-c` constraint when compiling requirements-papers.lock, so the
# lock describes the environment that actually ran rather than whatever resolves today.
#
# ONE lock serves all three papers because all three resolve from the same pyproject.toml with
# the same extra: figures_paper_a, paper_a.build, paper_b.build, figures_paper3 and paper3.build
# import nothing beyond numpy, scipy and matplotlib (verified by AST, and asserted by a test).
# Three per-paper locks would be three identical files free to drift apart.
#
# Regenerate the lock with:
#   uv pip compile pyproject.toml --extra figures \
#       -c docs/reproducibility/constraints-papers.txt \
#       -o docs/reproducibility/requirements-papers.lock
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.0
numpy==2.5.1
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.18.0
six==1.17.0
