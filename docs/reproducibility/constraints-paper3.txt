# Exact versions observed in the environment that PRODUCED the committed Paper 3 artifacts
# (figures, source-data CSVs, generated evidence tables). Recorded from `pip freeze` of that
# environment and used as a `-c` constraint when compiling requirements-paper3.lock, so the lock
# describes the environment that actually ran rather than whatever resolves today.
#
# Regenerate the lock with:
#   uv pip compile pyproject.toml --extra figures \
#       -c docs/reproducibility/constraints-paper3.txt \
#       -o docs/reproducibility/requirements-paper3.lock
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.0
numpy==2.5.1
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.18.0
six==1.17.0
