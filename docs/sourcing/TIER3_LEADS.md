# Tier 3 optional-upgrade leads

These items do not block current gates. They should be pursued after the hard gaps.

## Wadsworth segmented XCT volumes

Use the same follow-up as the PSD request. The institutional record is:
https://strathprints.strath.ac.uk/95930/

Ask separately whether the segmented volumes and lattice-Boltzmann domains can be archived
under a persistent DOI. Do not assume the article PDF's visual slices are the volume data.

## Mo / Ellero group

The group has published micro-CT/SPH work and the 2019 JFE manuscript is available through
institutional/author routes. A single request should cover:

- segmented micro-CT volumes;
- SPH input decks/parameter files;
- the dimensional definition and units of k1;
- the original direct/inverse-discharge series.

Keep `k^D` as the fallback until k1 is clarified.

## Grudeva

The repository already has a working reference dataset/configuration. First verify whether the
requested vial raw data are absent from the current public repo, then ask for replicate-level
measurements and the experimental-companion manuscript/data DOI.

## Egidi

Request the precise definitions and basis of rho and phi_s in Eq. 4. Until clarified, keep the
equation as a bracket/reference result rather than a quantitative EY gate.

## Foster

Ask for raw time-resolved CT volumes or reconstructed saturation fields. Published figures remain
digitizable, so this is an upgrade rather than a gate blocker.

## Fasano / ASIC

Request the Eq. 8.2 a,b,c coefficients together with the same 1993/1997 ASIC proceedings sought
for G2. Keep the existing figure digitizations qualitative until original coefficients are found.
