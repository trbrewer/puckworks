# Puckworks blocked-intake sourcing bundle

Open `BLOCKED_INTAKE_SOURCING_REPORT.md` first.

The bundle contains:
- exact transcriptions of two openly licensed source tables;
- a regression-consistency diagnostic rather than an unsafe derived-value grid;
- a quarantined density equation with an unresolved source conflict;
- schemas, protocols and request messages for blockers that cannot be responsibly filled from
  public data;
- a proposed status update for the repository ledger.

No source PDFs are redistributed.
