{
 "schema_version": 1,
 "purpose": "Append-only pre-freeze record of ledger wording displaced by a normative change, for entries in the IMMUTABLE initial claim ledger. The initial ledger (P0-G1a) exists so that claim drift during the analyses is detectable rather than reconciled after the fact; rewriting it to look as though the current estimand had always been the pre-analysis state would destroy exactly the evidence it was created to preserve. Displacement is therefore recorded here and the ledger is left byte-identical.",
 "ledger": "docs/paper1_resource/PAPER_A_CLAIM_EVIDENCE_LEDGER_V2_INITIAL.json",
 "append_only": true,
 "not_a_substitute_for": "P0-G1b final claim reconciliation, which is a separate gate and remains open",
 "entries": [
  {
   "delta_id": "D-01",
   "date": "2026-08-02",
   "claim_id": "C-OPS-01",
   "displaced_by": "docs/paper1_resource/PAPER_A_PIVOT_ANALYSIS_PROTOCOL_V2.md deviation record D-01",
   "field": "confirmatory_test",
   "recorded_wording": "P0-G8 interval classification at the compactified endpoint",
   "why_displaced": "The confirmatory test named a classification on the compactified domain running from 0.15 up to and including an infinite endpoint, where the reference quantity was an infimum over a domain the specified search never visited. Protocol V2 now references the threshold to J_ref on the finite D_WIDE = [0.15, 500] and evaluates the analytical endpoint separately, so the confirmatory test is the WIDE-referenced endpoint classification, not a compactified-domain minimum.",
   "replacement_wording": "P0-G8 WIDE-referenced endpoint classification: J_inf compared by interval against the threshold generated by J_ref on D_WIDE",
   "claim_status_change": null,
   "estimand_tag_change": null,
   "estimand_tag_note": "C-OPS-01 keeps FULL-WIDE. It reports a finite scan to kappa=500 and makes no endpoint statement, and FULL-WIDE is retained for exactly that. Only a result whose threshold is referenced to J_ref and which compares the separately constructed endpoint against it carries FULL-WIDE-ENDPOINT.",
   "scope_limiter_note": "The recorded scope limiter -- a finite-domain observation under one arbitrary tolerance, which does not establish an acceptable set with no upper limit -- is unchanged and remains correct. The WIDE reference does not weaken it: the fixed-positive-time limit that would license an eventual-upper reading is a separate derivation and is not closed.",
   "result_change": "none. P0-G8 has not run and docs/paper1_resource/PAPER_A_ASYMPTOTIC_PROFILE_LIMITS.json does not exist. This delta changes which quantity the gate will compute, not any reported number."
  }
 ]
}
