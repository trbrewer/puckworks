# RP-D-LC-001 — DECISION

```
DISPOSITION: INVALID_EXECUTION
evidence_use: DIAGNOSTIC_ONLY_INVALID_EXECUTION
cross_model_transfer_adjudicated: false
CROSS_MODEL_NUMERICAL_VERIFICATION · DETERMINISTIC_SYNTHETIC_GEOMETRY
NOT_EXPERIMENTAL_VALIDATION · NOT_REAL_PUCK_INFERENCE
NOT_A_REGISTRY_STATUS_PROMOTION · NOT_A_PUBLICATION_RESULT_YET
```

**Every recovery number below is DIAGNOSTIC ONLY.** None may be quoted as recovery, as
mechanism-only transfer, or as a weak quantitative success at any strength.

## Question

When the ground truth is generated by an independently implemented, spatially resolved 3D
creeping-flow solver rather than by `model1_two_path`, does the WP6-LC-IDENT boundary inverse
recover the fixture's independently calculated effective lateral-coupling number `Ξ`?

## Independent truth

Two constructions, kept distinct, neither calling the WP6 inverse. **Coupon-calibrated:** `a`, `b`
from x-periodic uniform-duct coupons; `G_bridge` from an axis-rotated bridge coupon.
**In-situ field-coarse-grained (primary):** four axial conductances from the blocked fixture's
internal fields on frozen surfaces; `G_lat_field = q_lat/(p1−p2)`;
`Ξ_field = G_lat_field·(1/A₁+1/A₂)`.

## Boundary-only inference

`infer_from_boundary` accepts exactly `{Q0, dP0, q1, q2, dP, orientation, converged}` and raises on
anything else; it imports `screen_wp6_lateral_identifiability.invert` rather than copying it; the
call order (infer, then evaluate truth) is asserted by AST.

## Fixture and solver

56×20×8 base template replicated S=2 and S=3 (exactly geometrically similar); `brewer2026.lb_reference`
D3Q19 TRT, magic Λ=3/16, float64, `tau_plus=2.0`, `g=2e-5`, Route A (periodic body force + resolved
common plenum), `p = rho/3 − g·x`. Aperture subset frozen from coupon output alone before any
full-fixture observable was inspected.

## Result — execution validity FAILED on three evidence-based controls

`primary_cause: componentwise_creeping_flow_control`

| control | verdict | evidence |
|---|---|---|
| `componentwise_creeping_flow_control` | **FAIL** | `Q/g` 2.38e-4 (S=2), 3.61e-4 (S=3); `Q0/g` 2.61e-4, 3.96e-4 — all above the frozen 1e-4 |
| `boundary_inference_forcing_stability` | **FAIL** | `Ξ̂` spread 1.42e-3 (S=2), 2.30e-3 (S=3); `Ξ_field` 2.23e-4, 3.62e-4 |
| `mass_conservation` | **FAIL** | S=3 plane-to-plane volume-flux spread 1.028e-3 vs 1e-3 (S=2 passes at 6.58e-4) |
| `frozen_C_linearity_control` | **FAIL** (reported, not required) | 2.6e-4 (S=2), 3.9e-4 (S=3) — preserved verbatim, never relabelled |
| `topology`, `coarse_graining_surface_stability` | **NOT_EVALUATED** | Arm J deliberately not run once validity had already failed upstream; fail-closed, and neither is reported as the cause |
| convergence · low-Mach regime · plane invariance · grid refinement | pass | |
| `backend_cross_check` | **NOT_EVALUATED** | Taichi absent, port cubic-only — recorded as not performed, never as passed |

**The drift is entirely in the flux, not the pressure.** `ΔP/g` is linear to **2.4e-8** (S=2) and
1.5e-7 (S=3) — essentially exact, as the periodic body-force construction requires — while `Q/g`
drifts at the few-1e-4 level. The conductance drift `C = (Q/g)/(ΔP/g)` is therefore the `Q` drift
almost entirely, which is why the earlier `{5,2}` conductance figure (2.60e-4) and the `{5,4}`
`Q0/g` figure (2.61e-4) agree so closely.

**The numerical gate is what caught it.** `boundary_inference_forcing_stability` has constant
inverse status, constant `sign(s−½)` and constant window/factor-of-two classification at every
forcing. Under a classification-only test it would have **passed**. It fails only because the six
spreads are bound to the frozen 1e-4.

## Two findings I did not anticipate

**1. The identical-path negative control fired, and it indicts the fixture.** With both lanes
identically ordered, `X = 0` exactly, the bridge pressure gap is `−0.0` and `q_lat = −1.2e-7 ≈ 0` —
so there is **no lateral driving whatsoever**. Yet opening the bridge still raised the total flow:

```
R − 1 = 0.01427     with zero lateral coupling
```

The aperture is a hole through the divider, so opening it does not only create a lateral path — it
**widens the axial channel**. That contribution is a pure artifact of the fixture geometry and is
present in every mirror case too.

**2. At large apertures the observations leave the two-node model's reachable set.** For the
measured `c_field = 0.3218`, the network's hard ceiling is `R − 1 ≤ c²/(1−c²) = 0.1155` at
`Ξ → ∞`. The largest frozen aperture gives `R − 1 = 0.1338` — **above the ceiling**. No `(c, Ξ)`
can reproduce that observation, and the inverse absorbs the excess by inflating `ĉ` (0.322 → 0.488),
which is precisely why `ĉ − c_field` grows from 0.038 to 0.166 and why `Ξ̂` saturates near 0.75
while `Ξ_field` spans 0.28 → 7.10.

## Diagnostic-only recovery numbers

| S | aperture | `Ξ_coupon` | `Ξ_field` | `Ξ̂` | `Ξ̂/Ξ_field` | `c_field` | `ĉ` |
|---|---|---|---|---|---|---|---|
| 2 | 3,2 | 0.140 | 0.289 | 0.176 | 0.609 | 0.322 | 0.360 |
| 2 | 3,4 | 0.471 | 1.099 | 0.489 | 0.445 | 0.322 | 0.362 |
| 2 | 5,4 | 1.175 | 2.844 | 0.742 | 0.261 | 0.322 | 0.398 |
| 2 | 9,3 | 1.815 | 3.360 | 0.712 | 0.212 | 0.322 | 0.454 |
| 2 | 9,4 | 4.044 | 6.730 | 0.747 | 0.111 | 0.322 | 0.488 |
| 3 | 3,2 | 0.131 | 0.283 | 0.168 | 0.593 | 0.322 | 0.356 |
| 3 | 9,4 | 4.071 | 7.098 | 0.751 | 0.106 | 0.322 | 0.484 |

`Ξ̂` is not even monotone in `Ξ_field` at S=2 (0.742 at 5,4 → 0.712 at 9,3). **None of this
adjudicates the transfer question**, because the truth it would be compared against did not meet its
own forcing-independence requirement.

## Controls that did behave

- **Path swap: perfect on all 10 rows** — `R` preserved, `s−½` sign reversed, `ĉ` and `c_field`
  reversed, `Ξ_field` and `Ξ̂` preserved.
- **One-voxel adversarial asymmetry:** biases `Ξ̂` by 5.6 % (plug) and 7.1 % (slab), with a
  blocked-share departure of ~1.3e-3; **the path-swap control exposes both**, and the inverse
  keeps returning a superficially physical `ok`.
- **Node nonuniformity:** node surfaces 1.83 % of `ΔP`; bridge faces **7.7 %** of the driving gap —
  the mid "node" is materially non-equipotential.
- **Grid refinement:** `R` moves ~2e-3 between resolutions; `Ξ_field` moves up to 5.5 %.

## Strongest alternative explanation

That the failure is an artifact of the *controls* rather than the execution — the same objection
that already forced errata E1 and E2. It does not hold here. `ΔP/g` is linear to 2.4e-8 on the same
rows, so the 1e-4 tolerance is demonstrably achievable by the very construction under test; the
failing quantity is the flux, measured the way the truth actually consumes it. The mass-conservation
control does have a genuine specification issue (volume vs mass flux, E4), and it is recorded — but
it is **not** used to soften that verdict, and the disposition does not depend on it: the
componentwise control fails independently at both resolutions.

## Disposition and why

**`INVALID_EXECUTION`.** The internal field quantities could not serve as sufficiently
forcing-independent numerical truth, so the comparison the tranche exists to make was never
licensed. **This is not a negative result about Ξ.** The cross-model transfer question remains
**unadjudicated**.

The fixture topology, exact voxel-index mirror symmetry, connectivity, absence of periodic lateral
bypass, the frozen pressure definition, τ-independence, convergence and the path-swap signature all
held up. What failed is a numerical-regime condition, and it is precise and remediable.

## Claim ceiling

Deterministic synthetic geometry · single-phase steady creeping flow · cross-model numerical
verification only · no experimental validation · no real coffee morphology · no real-puck `Ξ` · no
transverse coffee permeability estimate · no evidence that espresso occupies any `Ξ` regime · no
apparatus precision established · **no evidence-rung or registry-status promotion** · no claim of a
validated espresso digital twin · **card box 5 remains OPEN** · **box 6 remains closed on its
existing mathematical result** · **Paper 4 remains unauthorized**.

## Next action

A **separately frozen** `RP-D-LC-001b` at reduced forcing. Three changes, in this order:

1. **Reduce `g`.** Arithmetic minimum is ×4 against the 3.96e-4 worst case; ×8–10 gives credible
   margin. **The Re-scaling of the drift must be verified by that protocol's own forcing ladder,
   not assumed, and must not be used to rescue this execution.** Reduced forcing is also the shared
   remedy for E4: a smaller total pressure drop means a smaller density variation and hence a
   smaller volume-vs-mass flux residual.
2. **Redesign the bridge so it is a lateral path only.** The identical-path control shows the
   present aperture adds axial cross-section worth `R − 1 = 0.0143`. Either remove that degree of
   freedom geometrically, or calibrate it with the identical-path fixture at every aperture and
   subtract it — the second option must be predeclared, not fitted after seeing `Ξ̂`.
3. **Keep `Ξ_field` inside the model's reachable set** by design: require
   `R − 1 < c²/(1−c²)` with margin for every frozen aperture, checked from coupon output before
   the freeze.

Measure mass flux `∫ρu·dA` rather than volume flux. Arm J's 24 solves were not run and remain
available for that tranche.

## Reproduction

```
python -m puckworks.validation.slow.rp_d_lc_001 --backend reference --mode arm_a   --jobs 6 --output RUNDIR
python -m puckworks.validation.slow.rp_d_lc_001 --backend reference --mode coupons --jobs 6 --output RUNDIR
python -m puckworks.validation.slow.rp_d_lc_001 --mode freeze  --output RUNDIR
python -m puckworks.validation.slow.rp_d_lc_001 --backend reference --mode primary --jobs 6 --output RUNDIR
python -m puckworks.validation.slow.rp_d_lc_001 --mode assemble --output RUNDIR
python -m puckworks.analysis.rp_d_lc_virtual_fixture --write   # and --verify
```

## Source identity

`base_commit` `bbbc2b5f44be3bc5f0ee5951985e5f8594bbc6e1` · `protocol_freeze_commit` `06c1468`.
Per-stage execution authorities are recorded and **verified** in `result.json`
(`execution_authorities`): arm_a `9a4b5e8`, coupons `1c10c64`, primary `1c10c64`, each checked at
AST level (docstring- and formatting-insensitive) to confirm that no symbol it executed changed
after launch. Assembly commit and tree are recorded separately — the head that produced the numbers
is not the head that assembled them, and the two are never conflated.
