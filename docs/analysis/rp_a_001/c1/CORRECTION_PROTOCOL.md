# SCI-MD-003 / RP-A-001 bounded correction C1

Protocol `sci-md-003-rp-a-001/c1` is additive to the unchanged
`component-response-atlas/v1` specification. It freezes the correction method
before corrected comparative results are inspected.

The runner will construct validated explanation, result, eligibility,
prediction-interval, measurement-value, coverage, residual, and decision
records. Measurement value is evaluated per eligible pair, channel, and
scenario from executed response fields and explicit uncertainty. Conservative
bounded intervals are expanded by adding declared half-widths; no quadrature or
probability distribution is inferred. Only robustly separated intervals cover
a pair. The minimum-set algorithm returns every equally minimal set and treats
an empty eligible-pair universe as
`NO_ELIGIBLE_PAIRWISE_DISCRIMINATION_PROBLEM`, never as success.

Wadsworth mathematical executability is separated from evidence-domain and
adjudicative support. The ceramics-fit inertial closure is not validated on
tamped coffee, no supported pressure-gradient range is provided, and the
published espresso calculation is retained only as a non-adjudicative Fo_F
endpoint diagnostic. The 5/9/11-bar cases are unsupported for adjudication.
Fixed-porosity radius changes are constitutive sensitivities, not grinder
interventions, and do not enter grind-direction adjudication.

Foster saturation/front arrival remains native. First drip is excluded because
the separate scale-threshold measurement comparator does not establish a
source-backed saturation-to-scale time adapter with uncertainty. Cameron remains
a scalar-q downstream observer; arbitrary transient history transfer remains
unsupported.

The corrected export contract is `puckworks.response-atlas-export/v2`. Canonical
scientific payload bytes exclude operational duration and local paths, are
serialized with sorted keys and deterministic row order, and are SHA-256 hashed
without a circular self-hash. Verification regenerates every consumed payload
record and fails on protected identity or scientific-field drift.

Classification priority, residual eligibility, four-outcome decision rules,
seed 20260820, and claim ceiling are unchanged. The execution cap is 16 producer
evaluations; uncertainty realizations remain zero because no probability
distribution is declared. No OpenFOAM run, production-physics change, evidence
promotion, RP-C.1 activation, or merge is authorized.
