# SCI-MD-003 / RP-A-001 C1-R1 correction addendum

Protocol `sci-md-003-rp-a-001/c1-r1` is additive to the unchanged
`component-response-atlas/v1`, original RP-A protocol, and C1 protocol.

The runner shall derive exactly one of the four authorized outcomes through a
pure decision function using validated explanations, channel-specific
eligibility, component reports, comparisons, measurement records, coverage,
and minimum sets. Precedence is apparatus-null survival, then a unique robust
dynamic-bed route, then a unique spatial-only route, otherwise additional data.
Every decision retains a deterministic input hash, exact evidence identifiers,
structured nonselection reasons, and independent consistency recomputation.

Eligibility is keyed by pair, scenario, and channel. Measurement generation may
process only the named channel and must inherit scenario, comparability, and
adapter identity from exactly one eligibility record. Coverage points to exact
robust measurement records; no channel, scenario, or adapter inheritance is
permitted. Empty eligible universes remain scientifically incomplete.

The export contract is `puckworks.response-atlas-export/v3`. Canonical sorted
JSON excludes only operational duration and local paths; its SHA-256 is recorded
separately. The complete bounded pilot is rerun from a clean execution commit.
Seed remains 20260820 and the producer-evaluation cap remains 16. No OpenFOAM,
new physics, case redesign, EWP mutation, evidence promotion, RP-C.1 activation,
publication claim, or merge is authorized.
