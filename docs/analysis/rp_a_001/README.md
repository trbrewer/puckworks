# RP-A-001 retained atlas bundle

Run `python3 -m puckworks.analysis.response_atlas validate`, `run`, or `verify` from the repository root.
The protocol is additive to `component-response-atlas/v1`; generated compact outputs are retained here.
