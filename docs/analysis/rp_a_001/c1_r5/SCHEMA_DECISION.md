# Schema decision

Retain `puckworks.response-atlas-export/v5`. Existing v5 records contain the material identities and contexts needed for global closure; authoritative roots are repository inputs already bound by the manifest. C1-R5 strengthens validation and qualification protocol without adding a mandatory retained field or changing scientific artifact meaning.

If implementation proves any required ownership relation cannot be represented or reconstructed, work stops under the authorized schema/authority disposition rather than silently broadening v5.
