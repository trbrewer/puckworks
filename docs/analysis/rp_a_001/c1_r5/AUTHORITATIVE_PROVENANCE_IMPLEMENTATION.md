# Authoritative provenance implementation

Repository paths, not supplied records, anchor manifest, registry, card, protocol, case, assumption, inventory, explanation, result-cell, component-report, and residual provenance. Canonical question/contract/requirement/comparison constructors independently reconstruct semantic roots. Direct apparatus evaluation receives a separately reconstructed authoritative contract collection and requires exact equality, so changing caller provenance and recomputing caller hashes cannot authorize the mutation.

The production root chain is committed file bytes → stable SHA-256 → run-manifest selections → canonical explanations/questions/contracts/requirements/comparisons → owned lower evidence → reconstructed gates/apparatus/decision.
