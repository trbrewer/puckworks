# Mutation matrix

Mutate every globally material unselected class: dangling comparison/prediction, orphan cross-case and unauthorized-observable prediction, orphan contract/measurement/gate artifact, duplicate and wrong-type unused records, unauthorized root, caller-consistent provenance, selected-card/source-manifest hashes, cross-model/case/question ownership, coordinated derivatives plus orphan lower evidence, and ordering.

Every semantically invalid mutation must fail in producer and verifier at a semantic closure layer. Benign ordering must remain deterministic. Any accepted invalid mutation stops C1-R5.
