# Verifier independence

`independent_verifier.independently_validate_bundle_universe` separately loads and hashes repository roots, checks selected cards/components/explanations, indexes cells and gate/coverage records, and rejects orphan references. `_independent_global_universe` separately closes lower apparatus evidence and authoritative contracts before independent classification and gate reconstruction.

The verifier does not import producer governance or runner functions. It shares schema records, deterministic hashing, enums/constants, and low-level classification arithmetic only. Behavioral C1-R5 tests replace the producer global validator with a no-op and prove the verifier still rejects an orphan cross-case prediction; existing C1-R4 tests replace the producer apparatus evaluator and prove independent apparatus reconstruction remains active.
