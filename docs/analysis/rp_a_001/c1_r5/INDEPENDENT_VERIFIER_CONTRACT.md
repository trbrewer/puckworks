# Independent verifier contract

The verifier independently builds typed indexes, authorized roots, reference/context/ownership/provenance closure, classifications, gates, apparatus aggregate, downstream prerequisites, and decision. It must not import or call producer high-level universe validation or apparatus evaluation.

Only schema types, canonical byte/hash primitives, immutable constants/enums, and low-level arithmetic may be shared. Behavioral tests will replace producer global validation and apparatus evaluation with failures/no-ops and prove verifier rejection of G1, G2, G3 and derivative-chain mutations.
