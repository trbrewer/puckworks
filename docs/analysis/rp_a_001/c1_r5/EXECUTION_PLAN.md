# Execution plan

Implement producer global closure, authoritative ownership/provenance, separate verifier closure, and focused regressions in additive commits. Run reached-path and global mutation coverage. Commit all tracked protocol/finalization documentation, freeze the candidate, then perform dual detached replay, field comparison, complete declared-extra QA, normal push, exact-SHA CI, and external/GitHub handoff without another candidate commit.

The actual-pilot result is expected but never targeted. Scientific drift, nondeterminism, accepted mutation, undefinable ownership/provenance, or required out-of-scope change invokes the narrow authorized stop.
