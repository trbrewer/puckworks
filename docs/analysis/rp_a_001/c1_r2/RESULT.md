# SCI-MD-003 / RP-A-001 C1-R2 result

The v4 runner executed nine bounded producer evaluations and emitted 45 result
cells with zero numerical failures. Numerical component responses and the exact
Wadsworth nested closure are unchanged from v3; this correction changes the
scientific governance contract, not model equations.

Four potential discrimination requirements were retained at exact
pair/scenario/intervention/basis identities. None is relevant to final
discrimination because the underlying comparisons are ineligible or unresolved.
Consequently there are zero eligible channel records, zero measurement records,
zero coverage edges, an empty relevant universe, and a non-vacuous
`NO_COMPLETE_MEASUREMENT_SET`.

The apparatus evaluation is `NOT_EVALUATED` with reason
`NO_MATCHED_APPARATUS_COMPARATOR`; absence of matched apparatus evidence is not
treated as failure. Apparatus-gate coverage and global discrimination coverage
are both incomplete. Decision rule `rp-a-001-decision/v2` therefore derives
`SCI_MD_003_RP_A_001_ADDITIONAL_DATA_REQUIRED`. Dynamic and spatial outcomes are
ineligible because apparatus was not robustly ruled out, not because those
physics were disproven.

Physical validation remains `NOT_ESTABLISHED`. No global winner, consensus
model, production physics, experiment, EWP repin, RP-C.1 work, or inference
against lateral coupling follows.
