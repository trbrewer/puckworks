# Execution plan

Implement evidence indexing/validation and producer recomputation; add a separately implemented verifier; convert `REVIEW-C1R3-001` into production-path regressions; run mutation campaign; replay twice; retain C1-R4 provenance/result records without overwriting C1-R3; qualify locally; push additive commits; await terminal exact-head CI; hand off to a new independent reviewer.

No outcome targeting is permitted. Material actual-pilot drift triggers investigation/stop rather than logic adjustment.
