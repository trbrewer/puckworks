# RP-A-001 initial-candidate supersession

The initial candidate is preserved in Git and is not eligible as the corrected
SCI-MD-003 / RP-A-001 result. Its response-atlas export v1 used manually assigned
measurement support, an aggregate pseudo-pair, insufficient source-domain checks,
and verification that did not protect the consumed export. C1 supersedes rather
than rewrites that evidence.

Canonical corrected artifacts live under `c1/`. The original commits, bytes, and
hashes remain auditable through `superseded/initial_candidate/manifest.json`.
