# Activation

The existing clean worktree matched the expected branch, R0 head and tree. The original base is an ancestor; `origin/main` is unchanged; no remote branch exists. The preserved design, export-publication, and SCI-MD-007 authorities were verified exactly. The absolute worktree path is evidence only and is excluded from portable exports.
