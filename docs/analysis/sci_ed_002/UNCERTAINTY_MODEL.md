# Uncertainty model

The JSON contract enumerates the complete hierarchy and components. Primary estimates report standard and expanded uncertainty, coverage, degrees-of-freedom method, component contributions, version, and quality. Raw replicates remain available for recomputation. Moisture is linked at material-batch level; injections remain nested. Ratio covariance is estimated from the joint preparation/adjacent-aliquot structure and never presumed zero without evidence.
