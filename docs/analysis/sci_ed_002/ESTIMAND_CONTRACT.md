# Estimand contract

The normative machine contract is `ESTIMAND_CONTRACT.json`. `T_total` and `I_ref` remain distinct method-conditioned dry-coffee-basis quantities. Their ratio preserves covariance and is not clipped. `Q_production_solid_initial` and `c_s0` remain `NOT_ESTABLISHED`; subtraction, ratio, adapter, prior, runtime default, or equivalence is prohibited.
