# Independent scientific review — SCI-MD-MORONEY-TRANSFER-001

Reviewer: independent review agent, separate from implementation author. Date: 2026-09-24. Scope: substantive pre-scoring review and final review of the proposed **unscored preparation** outcome; no real-data calibration or target scoring authorized or performed by this reviewer.

Reviewed producer: `Puckworks task worktree (repository root)`.

- Base commit: `e95d4585aa2dd9ab6eaa9da4f6aa8d311109e5ba`.
- Base tree: `f806e8d4dfc8cf7a0efe323d919536d1e6e7b221`.
- Reviewed staged candidate tree: `6d101cc1c305f57a35accc59e1ce88d30c330b45`.
- Reviewed `freeze.json` SHA256: `661c35d1c45f7b6be065925bdd89f11cc06124b1522c8198edab5a358b981d2b`.
- All dependencies listed by that freeze matched their hashes at review.
- EWP consumer base identified by the contract: `09cdda7fb4b28229be34915a3371c79229316cee`, tree `6d962e653ab30e3e191e09edb842718d6df76bc0`. This review addresses the scientific producer and the allowed consumption ceiling, not a separate exhaustive EWP diff audit.

## Disposition

**NOT APPROVED FOR CALIBRATION/PREDICTION/SCORING.** The original figure-object gate remains open, and the candidate has bounded implementation defects listed below. The proposed thresholds are accepted individually as engineering decision tolerances, but this exact executable freeze is rejected for scoring. There is no approval JSON and no independent PASS for the source contract.

The conservative research formulation is scientifically coherent enough to continue as a conditional, retrospective same-source comparison after these corrections. An honest preparation-only conclusion of **NOT_ADJUDICATED / PHYSICAL_VALIDATION NOT_ESTABLISHED** is supported. The current RESULT statement that the figure-object gate is the *only* blocker is premature until the implementation findings are resolved. This is a G2 research numerical-method producer with G1 source/freeze and prospective G3 scoring, even though EWP consumption changes only documentation. No production physics, defaults or registry promotion follows.

## Materials and source limitation

Read AGENTS.md, CLAUDE.md, onboarding, minimum-necessary governance, the data-guide entry material, Moroney register/manifest entries, the 2015 card and batch implementation, task protocol/JSON, RESULT, freeze/identities, qualified mapping, support, verification records, both new source modules and all task tests. The packaged `moroney2015/data` CSV family was inspected; other external families were not used. Historical Moroney 2016/2019 reuse is duplicate lineage, not independent validation.

The review has **not inspected an original Moroney-2015 PDF**. The preparation records report no PDF in the configured Moroney inventory, public endpoint failures, and rejection of a similarly named 2017 PDF. These are environment/access limitations, not proof that the source is unavailable everywhere. Neither CSV coordinates nor public extracted text can establish original PDF object identity. The four Figure 11 candidates, one-based data rows 7, 33, 45 and 71, remain **suspected** legend entries; the original page-233 visual/vector check and analogous selected Figure 3 audit are not passed. I neither certify these exclusions nor invent PDF page/object coordinates. No residual-based exclusion is permissible.

## Scientific assessment

The mass-state derivation is consistent: positive internal-to-mobile exchange appears with opposite signs in the two reservoirs; surface loss appears as mobile gain; finite-volume advection telescopes into the accumulated outlet mass. The old card's printed-sign ambiguity is explicitly resolved using the existing conservative batch convention. Grain and mobile volume factors, mg/g versus kg/m³, delivered mass and pot concentration conversion are consistent. Pot data constrain delivered solute, not total extraction. The first-outflow origin and no artificial line from zero to the first outlet marker are appropriate for this bounded comparison.

Both volume branches reconcile finite dose rather than silently creating soluble mass. The dose branch preserves dry grain porosity and conditions mobile volume on geometry; the hydraulic branch conditions mobile volume on an assumed KC closure and derives an effective dry pore fraction. Neither validates permeability. Initial mobile solute is debited locally from the surface allocation; kernel solute is already dissolved; total initial inventory is fixed. The uniform/linear amplitude families are admissible over the declared parameter bounds and do not fit a target startup amplitude. This finite family does not identify wetting or exhaust startup uncertainty. The equation and initialization assumptions must remain visible in any eventual transfer verdict.

N_M and N_T share a bounded cumulative model, analytic derivative, training objective and deterministic start policy. Their distinct dose/time width transfer is explicit and scientifically interpretable. Treating pot and outlet as dependent observations, retaining signed negative digitizations, avoiding point bootstraps, and reporting source-model lines only as fitted references are appropriate. Comparison fairness depends on retaining failed baseline fits and qualifying comparator completeness, which the current decisions fail to enforce.

The functional calibration API does not accept targets, and the prediction phase reads target mass coordinates only after the source export. The leakage tests support that separation. This remains retrospective public-curve model development, not genuinely blind validation, independent replication or an espresso mechanism claim.

## Threshold decision

I **accept** 5 mg/g outlet RMSE and 1.0 dry-dose EY percentage point maximum cumulative error as predeclared adequacy budgets for this engineering screen. I **accept** the joint requirement of at least 20% and at least 1 mg/g outlet improvement against **each** empirical hypothesis. I **accept** at most 0.1 EY percentage point cumulative-error regression against either comparator. These are practical decision tolerances, not source measurement-error estimates or statistical significance thresholds. They support prioritization only, not unique physical mechanism identification or EWP validation.

I also accept the declared 0.5 mg/g and 0.1 EY point numerical qualification limits with the explicit limitation that adjacent-grid/tolerance differences are empirical sensitivity measures, not rigorous continuum-error bounds. Since the absolute gain threshold is only twice the numerical outlet allowance, applying numerical uncertainty to the gain margins is essential. The source perturbations are acceptable finite readout probes; they are not experimental confidence bounds. The present code attempts conservative interval decisions but needs the support/completeness/category corrections below.

## Required bounded corrections

1. **Reverify actual frozen dependencies immediately before scoring.** `moroney_transfer_run.py:213–220` checks the manifest's hash and prediction artifacts but does not check the actual files listed inside the manifest. A changed target CSV, qualified mapping, protocol or executable can therefore be consumed with an unchanged freeze manifest. Reuse the dependency check before attaching observations and record/reverify executable/environment identity as applicable. This directly protects the scientific scoring surface, not speculative governance.

2. **Cover every transformed observation coordinate and compute numerical allowances on that support.** At line 170 the common prediction support includes only untransformed deep coordinates, while high-readout deep scoring multiplies them by 1.002. In this candidate the grid ends at 985.81 g, but transformed deep support reaches 987.78162 g. `np.interp` silently uses its endpoint value beyond the domain; internal perturbed deep points are also merely interpolated between sparse tail points. Include transformed deep and shallow coordinates and reject interpolation outside support. At lines 201–202 outlet numerical error is evaluated on central coordinates even for perturbed variants; evaluate each variant on its actual scoring support. Delivery sensitivity over the whole common grid is conservative, but should be distinguished from the declared observed-support metric.

3. **Do not declare gain with missing empirical perturbation fits.** `decide()` requires every mechanistic perturbation fit but only a nonempty list of baseline scores. A synthetic reproduction with successful mechanistic fits, only central empirical predictions, and failed low/high empirical fits returned `TRANSFER_ADVANTAGE_FOR_TESTED_SOURCE_CONDITIONS`. Require complete comparator coverage; expose baseline deep adequacy and optimizer outcomes. A weak baseline may be a reported result, but missing/unsuccessful calibration must not vanish from an apparently complete comparison.

4. **Separate parameter/startup disagreement from source/numerical threshold straddling.** Current interval aggregation pools all admitted starts and perturbations. A synthetic case with two admitted starts, one adequate and one inadequate, no numerical uncertainty and one source variant returns generic `UNRESOLVED`. The protocol promises `INITIALIZATION_OR_PARAMETER_LIMITED` for that case. Preserve the hierarchy of per-start, per-source and numerical decisions and report consequential variation explicitly, including distinction between volume-basis and startup-family changes. The conservative current result avoids a false PASS but does not implement the promised scientific diagnosis.

5. **Enforce both original-figure audits in source readiness.** `qualify()/freeze()` currently allow source_ready to become true once the four Figure 11 row strings occur in a confirmation file. They do not require the selected Figure 3 visual/object audit. The independent human review remains the main defense, but the readiness statement itself must require inspectable evidence for both figures, original PDF identity, page/object coordinates, axis interpretation and observation-versus-legend classification. Do not turn pending candidates into factual legend roles before that evidence exists.

6. **Repair and smoke-test the actual result-rendering path before calling the CLI executable.** The implementation author independently reported that `figures()` constructs `Producer` without required `result_map`. Include this bounded fix and a fully synthetic predict-artifact/score/figure smoke test. The existing tests exercise solver/calibration functions, not the complete decision/rendering path. No real target values are needed for this check.

Additional bounded reporting corrections: `score()` currently uses dose-branch mobile volume for every early/post-wash split, including hydraulic families. Either declare a common reference window explicitly or use the relevant branch and label empirical comparators consistently. The closed-exchange test checks an algebraic RHS relation and conservation, but does not currently compare a beta=0 closed exchange trajectory to its exponential analytic solution; add that small analytic control if claiming an analytic closed-exchange verification. Failure counters are aggregate; retain per-start failure causes/parameters if continuing to claim that every solver failure is recorded.

## Verification and review closeout

Reviewer executed `python -m pytest tests/test_moroney_transfer.py -q` in the supplied environment: **15 passed in 2.02 s**. Additional reviewer-only synthetic checks reproduced the missing-baseline PASS and parameter-disagreement classification defects. Coordinate-only inspection reproduced the transformed-deep support overrun. No real target scores, fits, residuals or scientific transfer predictions were generated. No repository implementation was modified by this reviewer.

The three-grid primary controls show excellent conservation; their large pointwise startup-front maxima must remain visible rather than being confused with the observed-support RMSE qualification. Fit-specific mesh and tightened-tolerance qualification remains mandatory and unexecuted. A finite-volume analytic advection chain, units/inventory/boundedness checks, zero-transfer and zero-dissolved controls give useful verification. They do not validate source observations or transfer physics.

After one bounded pre-scoring correction, an addendum can verify only affected bytes/tests and close implementation findings for the **unscored preparation** outcome. The source gate must remain closed unless original Figure 11 and selected Figure 3 evidence actually becomes available. EWP may consume the honest NOT_ADJUDICATED preparation conclusion; it may not interpret this review or green synthetic tests as permission to integrate fitted parameters, score targets, claim failure/advantage or promote physical-validation status.
