# SCI-MD-007-R2 evidence-package correction contract

This prospective additive contract freezes the bounded R2 correction authorized on 2026-08-26. It preserves the original feasibility contract and R1 search contract byte-for-byte. R2 performs no new literature search, adds no candidate or scientific observation, and does not change eligibility, evidence lineage, or validation-group linkage.

The authoritative reducer must close the existing search registers relationally, apply every frozen F4 primitive—including per-species within-group quantitative roast variation—and calculate F7 leakage from actual folds. Species, harmonized-roast, and metric-type support are transportability diagnostics, not new F7 gates. Laboratory identity affects laboratory-independence counts but is not a validation-group union edge.

The scientific evidence registers remain `BYTE_IDENTICAL_SCIENTIFIC_EVIDENCE_REGISTERS`. Any required search-bookkeeping correction must be mechanically derivable from existing tracked rows and recorded explicitly. An unresolvable inconsistency produces `SCI_MD_007_R2_REGISTER_CLOSURE_STOP`.

The result schema is `1.2.0-R2`. Feasibility and disposition are reduced using the unchanged formulas and thresholds. No real-evidence predictor may run in R2, including on an unexpected feasibility PASS.

Puckworks remains authoritative for evidence-register validation and gate primitives. EWP will verify exact producer Git authority and package bytes and independently reduce only the exported Boolean feasibility decision. After terminal-green producer and consumer candidates are frozen, a reviewer independent of implementation must conduct the coordinated review. Neither draft PR may be merged under this authorization.
