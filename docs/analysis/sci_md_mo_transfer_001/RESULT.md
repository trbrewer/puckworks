# SCI-MD-MO-TRANSFER-001 — implemented, unscored result

**BLOCKED_SOURCE_CONTRACT on both axes; numerical application separately
NUMERICALLY_UNRESOLVED.** Retain current production behavior. Neither PSD-informed
release nor intraparticle diffusion has earned or lost development priority on
this task. No candidate is rejected as physical law. No transfer score was run.
This is an executed source audit and conservative reference verification, not a
completed fitted transfer experiment. The prerequisites did not pass.

The increment is a research-only extensive-state kernel implementing exactly S0,
S2 and D2 with common transport/observer, a stable 51-row source register, bounded
fit/scoring primitives and synthetic tests. Historical Fig8 reconstruction,
registered models and gates remain unchanged; their within-bars result is not
relabelled as transfer. The runner deliberately stops before real fitting; its
primitives are preparatory, not released source-qualified orchestration.

## Source and observations

The [source contract](SOURCE_CONTRACT.md) and independent
[source review](review/SOURCE_REVIEW.md) pin the original author manuscript,
methods/equations/figures/appendix and cited apparatus patent. Six mounted family
CSVs match canonical bytes. The 30-page institutional PDF was recovered and
inspected; its SHA256 is
`9e3f99a97530ec4b0936b8295f5b3e4485614dcbd25ef3f3c2dbbae0c8afdd82`.
The typeset publisher PDF, raw replicates and campaign controller/storage trace
were not inspected. Their absence here is not nonexistence or corpus exhaustion.
No protected target was accessed.

All51 supplied rows across nine E/M/F×2/3/4 conditions are retained for source
inspection; **zero qualify for fitting/scoring**. E/4 has only 3 rows, ending 20 g;
the other eight have 6. The original Fig7 visibly has later E/4 markers, but no
new digitization was performed and no support was invented. H contributes PSD
only. Simulated curves, partition sweeps and flow-decay output are excluded.

The missing source facts are the campaign's cup/whole-basket split and
mass-to-time mapping, startup inlet/storage protocol under outlet-flow control,
and unambiguous experimental yield denominator/averaging operator. The printed
Eq37 uses initial soluble inventory, while the methods invoke powder mass.
Powder transfer additionally lacks common-material/shared-inventory authority.
The owner's per-powder-inventory alternative cannot repair the flow operator.
No fit selected among the 15 g pooled/7.5 g single-shot/inventory-normalized
alternatives. Source density/porosity assumptions are not shot measurements.

## Executed numerical evidence

The hypothetical dry-fill observer is explicitly conditional: 15 g whole basket,
half collection, source geometry, nominal porosity 0.17 and density 1000 kg/m3.
It is used only to test predictor equations, never to qualify experimental mapping.
Three fixed representative/extreme parameter cases per candidate were run at
24/48/96 axial cells and 12/24/48 radial shells, plus tightened 96/48 tolerance.
Actual tabulated mass coordinates were inputs; no response values entered these
checks. All inventories were 1, testing the physical ceiling without optimizing.

- 36 bed trajectories launched:32 passed;4 rejected after integration by the
 positivity/monotonicity check. No automatic retries or optimization were run.
- 2412 internal BDF phase solves completed:2144 from returned trajectories and
 268 reconstructed from the four post-loop rejections (25+49+97+97). Rejected
 extrema/RHS counts were not retained by the original exception; they are unknown.
- Three additional radial perfect-sink solves completed. Analytical remaining-mass
 errors at 24/48/96 shells were 0.00261643/0.00064303/0.00016011.
- Four legacy diagnostic trajectories completed. Campaign total: 43 launched,
 39 passed/returned, 4 rejected; 2419 BDF solver calls including radial and legacy.
 Unit-test replays are separate from these campaign counts.

For the 32 returned bed trajectories, maximum relative solute residual is
3.47e-11 and water residual4.09e-17. Their minimum normalized state is
-3.29e-9, within the explicitly reported 50×atol tolerance of its trajectory;
no negative state is clipped. Pre-breakthrough delivered solute is exactly zero.
These conservation results do not waive the four failed positivity/monotonicity
checks. S0's fast coarse/medium runs and S2's fast fine/tight runs were rejected.

Empirical medium-to-fine plus fine-to-tight output differences exceed the 0.10 EY-pp
ceiling for S0 middle(0.1215), S2 middle(0.1845), and D2 slow/middle/fast
(0.8823/0.3890/0.5043). Strength allowances are derived consistently at each mass.
No convergence order is assumed. **No fitting resolution is selected or qualified.**
The campaign is not expanded to seek a passing grid. Full records, failed cases,
radial controls and numerical plots remain in the bundle.

The legacy coarse 8×8 diagnostic gives pre-breakthrough eluted fraction 0.03128,
maximum balance deviation 0.17969 (including initialization), ignored dose argument,
and scale ratios 2 for EY but 1 for strength. These are diagnostics of that
configuration, not changes to historical gates or a source-paper reproduction.

## Candidate decisions and uncertainty

| Candidate | Training | Flow transfer | Powder transfer | Decision |
|---|---|---|---|---|
| S0 | NOT_EXECUTED | SOURCE_BLOCKED | SOURCE_BLOCKED | unresolved |
| S2 | NOT_EXECUTED | SOURCE_BLOCKED | SOURCE_BLOCKED | unresolved |
| D2 | NOT_EXECUTED | SOURCE_BLOCKED | SOURCE_BLOCKED | unresolved |

Optimization starts/evaluations/cache hits=0; real prediction trajectories=0;
scoring passes=0. Empty fit/prediction/score receipts mean not executed, not zero
error. No selected simpler comparator, adequacy claim, material gain or independent
transfer failure exists. No residual plot is fabricated; nine separate plots show
the retained observations and explicitly state that predictions are unavailable.
The strength plot audits the two source dose alternatives; it does not infer dose.

Published three-brew SD, zero/unknown digitized bars, rounded coordinates and
unquantified digitization/mapping uncertainty are distinct from numerical error.
The 51 rows and paired summaries are not independent experimental samples. No
confidence intervals or population-level superiority claims are made.

## Reproduction, authority and closeout

`identities.json` records freshly fetched main and owner checkout commit/tree
identities; isolated worktrees preserved owner work. Exact task issue/PR searches
found no equivalent task. The linked issues are Puckworks#273 and EWP#179.
`freeze.json` hashes the code/inputs/contract and compact evidence; no real
pre-scoring approval is claimed. Independent acceptance and live CI are separate
PR records. The EWP HANDOFF pins the exact submitted producer and unchanged lock.

Commands: `python -m puckworks.analysis.mo_transfer_report audit` reproduces the
register; `verify` performs the explicitly hypothetical bounded numerical campaign;
`legacy` generates diagnostic evidence; `figures` renders compact plots. The
`mo_transfer_run` entry point raises BLOCKED_SOURCE_CONTRACT. Verification replay
is documented for audit, not automatically executed again by this task.

Claim ceiling: retrospective source-conditioned model development only.
**PHYSICAL_VALIDATION_NOT_ESTABLISHED; EWP architecture NOT_ADJUDICATED;
native integrations/builds=0.** No production defaults, dependency lock or
registered physics changed. No merge, new experiment, contact, laboratory
programme or successor is authorized or executed. Stop with this bounded result.
