# Scoped preflight (2026-09-25)

Clean main-based worktrees, created without modifying unrelated worktrees:
Puckworks `09ee96b56220b5db75051efebbb575b9b1cb49d8`,
EWP `064d7a7894127ccc389d8d3a03b66da0a3ada27a`.
Both origin/main refs were fetched before branching. Live Grudeva issue/PR search
found the existing solver issue #67 and merged rights PR #239; no exact
individual-shot TIME/MASS/MIXED/TEMPLATE comparison. Current main includes merged
Mo work (#274/#180); this branch imports no unmerged scientific work.

Controlling governance read in both repositories: AGENTS/onboarding,
minimum-necessary-governance (identical), Puckworks CLAUDE/ROADMAP/SPRINTS and
available-data-first policy/data guide/MANIFEST/capability register, EWP current
state/claim ceiling/data-first plan/controlling strategy/current leverage
programme and ledger. Reviewed the retired-candidate record and relevant prior
Grudeva source/gates plus analysis/fitting/report utilities. No retired question
is reopened. Puckworks owns implementation/data/canonical results; EWP owns only
research consequence and exact producer handoff. Existing source and static
checks, full quick tests, registry gates and ordinary CI remain required.

The new task satisfies the data-first three-test gate:

1. New information: the existing joint mass/TDS measurements are now used as
   individual physical-shot groups for a matched coordinate comparison; the
   historical aggregate reconstruction does not answer this question.
2. Consequence: adequate material mass gain can change the preferred reduced
   research predictor; adequate null retains a simpler coordinate; inadequate
   forms do not earn adoption; a named source/numerical block limits its axis.
3. Grinder-to-cup relevance: predicts conditional delivered fraction chemistry
   given measured beverage collection, without claiming flow prediction or an
   inventory/permeability/pressure bridge into EWP.

The local configured corpus inventory was checked, not inferred from Git.
The raw CSV and analysis notebook were absent from that local family, so the
specifically authorized public files were retrieved at the pinned upstream
commit. Source file contents and notebook cells were actually inspected.
The original thesis methods were also inspected (first host returned HTTP 403;
the institution's pure.port.ac.uk copy succeeded). No notebook was executed.

Reusable infrastructure: existing repository module CLI/test/document conventions,
source discovery's explicit path precedence, NumPy/SciPy core dependencies,
source hash/freeze/review/prediction/once-only score pattern from bounded analysis
utilities, task-local VizSpec. No general framework, new dependency or production
component. The delivery equation is implemented afresh; the registered Grudeva
solver and its all-data fitted coefficients are context only.

Source count/eligibility and permission boundaries are in PROTOCOL/SOURCE_AUDIT.
No missing-data/laboratory escalation, no claim of corpus exhaustion, and no
mechanism or independent-validation upgrade follows from this task.
