# RP-A-001 blocked handoff

`RP_A_001_RESUME_NOT_AUTHORIZED`

The three-component RP-A pilot must not restart from this review. Wadsworth passes the bounded use profile, but Cameron lacks affirmative clearance for public reduced outputs derived from SI Tables S1-S5. Foster’s synthetic-pressure path appears potentially supportable for noncommercial derived outputs, but the current global policy cannot represent that narrow path without over-clearing the DE1/private-trace and Cameron-dependent paths.

Required resolution:

- Cameron: obtain written permission or a new affirmative licence covering local model use and public reduced derivatives, curves, plots, and summary tables derived from the embedded SI arrays. The covered material must expressly include Tables S1-S5 or the transcribed numerical values used by `extraction_bdf.py`.
- Foster: identify the exact input profile RP-A will use. If it uses a recorded trace, establish that trace’s outward-use permission. If it uses `k_from_kappa`, resolve the Cameron dependency. If it uses only synthetic prescribed pressure and independently specified SI permeability, authorize a separate, non-bypass policy change capable of representing that scope without clearing other paths.

No author or publisher was contacted. Component substitution requires new user authorization.

If a later review authorizes all three components, the future RP-A agent must preserve the stopped worktree for audit until owner-approved cleanup, fetch then-current `origin/main`, verify the rights-review merge commit is an ancestor, create a fresh branch and worktree, repeat collision checks, re-freeze Commit A prospectively, use only cleared inputs and outputs, incorporate notices, execute every row from scratch, and consume no pre-clearance scientific output.
